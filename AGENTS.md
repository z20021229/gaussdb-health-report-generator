# AGENTS.md

## 项目定位

本仓库用于实现 GaussDB 数据库季度巡检报告自动生成工具。工具应从用户提供的巡检包中提取客户信息、数据库信息、节点信息、性能指标、容量指标、风险项和检查结论，并按照既有 Word 样例的格式与交付风格生成正式报告。

## 强制规则

- 禁止硬编码样例报告或样例巡检包中的具体数据。
- 所有客户信息、节点 IP、数据库名、巡检日期、检查结论都必须来自输入文件。
- 如果无法解析某项内容，统一写“未采集”或“未提供”，不能编造。
- 报告正文面向客户交付，措辞要正式、简洁、专业。
- 解析器必须通用，不能依赖单一客户文件名、单一 IP、单一节点数量。
- 长明细只展示 Top N，完整原始内容保留到中间文件或附录路径。
- 每次修改后必须更新 README 和测试。
- 优先保证可运行，再逐步提升解析覆盖率。

## 样例文件使用边界

`templates/GaussDB数据库健康诊断报告.docx` 仅可作为以下内容的参考：

- 报告格式
- 章节结构
- 标题层级
- 表格样式
- 正式交付措辞风格

禁止从样例报告中复制或固化以下内容：

- 客户名称
- IP 地址
- 数据库名
- 磁盘使用率
- CPU、内存、连接数等指标
- 巡检日期
- 风险结论
- 具体整改建议中绑定客户环境的数据

`samples/收益所有人.rar` 仅可作为解析器开发和测试样例。禁止依赖该压缩包的客户名、文件名、目录结构细节、IP、数据库名、节点数量或巡检结果来实现特殊逻辑。

## 开发原则

1. 先建立稳定的输入、解析、中间结构、报告生成流程。
2. 解析结果统一落到结构化 YAML，作为报告生成的唯一数据来源。
3. Word 报告生成器只能读取结构化数据，不直接从原始巡检包拼接正文。
4. 解析失败、字段缺失、格式不匹配时应记录原因，并输出“未采集”或“未提供”。
5. 对于多节点、多实例、多数据库、多文件格式场景，默认按集合处理。
6. 对于大日志、大 SQL 明细、大对象列表等长内容，报告正文只展示摘要和 Top N。
7. 中间文件、解析日志、完整原始明细应保存在可追溯路径中，便于复核。
8. 测试样例应覆盖正常输入、缺失字段、不同节点数量、不同文件格式和解析失败场景。

## 后续开发建议

建议按以下阶段推进：

1. 项目骨架和命令行入口。
2. 巡检包解压与文件清单扫描。
3. 通用文本、日志、HTML、表格解析能力。
4. 结构化 YAML schema 定义。
5. 风险分析与结论归类。
6. Word 报告模板映射与生成。
7. 样例巡检包回归测试。
8. 多客户、多版本、多节点场景增强。
